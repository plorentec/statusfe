<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= title %> - StatusFe Admin</title>
<link rel="stylesheet" href="/css/admin.css">
<style>
.status-upcoming { background: rgba(99,102,241,0.2); color: #818cf8; }
.status-ongoing { background: rgba(245,158,11,0.2); color: #fbbf24; }
.status-completed { background: rgba(16,185,129,0.2); color: #34d399; }
</style>
<script>document.body.classList.add('dark');</script>
</head>
<body>
<div class="admin-layout">
<%- include('../partials/_sidebar'); %>
<main class="admin-main">
<div class="admin-header"><h1><%= title %></h1>
<% if (message) { %><div class="flash flash-<%= messageType %>"><%= message %></div><% } %>
</div>

<div class="card-header-row">
<h2>Maintenance Windows</h2>
<a href="/admin/maintenance/new" class="btn btn-primary">+ New Maintenance Window</a>
</div>

<% if (windows.length === 0) { %>
<div class="card"><p class="empty-state">No maintenance windows scheduled.</p></div>
<% } else { %>
<div class="table-container">
<table class="admin-table">
<thead><tr><th>Title</th><th>Page</th><th>Component</th><th>Status</th><th>Starts</th><th>Ends</th><th>Actions</th></tr></thead>
<tbody>
<% windows.forEach(w => { %>
<tr>
<td><strong><%= w.title %></strong><br><small class="text-muted"><%= (w.description || '').substring(0, 50) %><%= (w.description || '').length > 50 ? '...' : '' %></small></td>
<td><code><%= w.page_id ? w.page_id.substring(0,8) + '...' : '-' %></code></td>
<td><%= w.component_id ? w.component_id.substring(0,8) + '...' : '<span class="text-muted">All</span>' %></td>
<td><span class="status-badge status-<%= w.status %>"><%= w.status %></span></td>
<td><%= w.starts_at %></td>
<td><%= w.ends_at %></td>
<td class="actions">
<a href="/admin/maintenance/<%= w.id %>/edit" class="btn btn-sm">Edit</a>
<form action="/admin/maintenance/<%= w.id %>?_method=DELETE" method="post" class="delete-form" onsubmit="return confirm('Delete this maintenance window?')">
<button type="submit" class="btn btn-sm btn-danger">Delete</button>
</form>
</td>
</tr>
<% }) %>
</tbody>
</table>
</div>
<% } %>
</main>
</div>
<script src="/js/admin.js"></script>

</body>
</html>
